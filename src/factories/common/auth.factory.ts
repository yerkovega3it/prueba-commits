import type { UserAuthConfig, UserAuthMenuSidebar } from '@/interfaces'

export const initialUserConfig: UserAuthConfig = {
  darkTheme: false
}

export const initialUserMenuSidebar: UserAuthMenuSidebar = {
  main: [],
  admin: []
}